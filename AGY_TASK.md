# AGY_TASK — Автопилот Batch Y: Phase 2 Depth F (5-domain calibration + ritual 15м + streak shield)

Ты руки Fokus. Репо: `/workspace/developer/github/Fokus`.
Игнорируй Eligibility. Без подтверждений. agy 1.1.1 + `--dangerously-skip-permissions`.
Не мержи PR. Не коммить `AGY_TASK.md`.

Цель: углубить Phase 2 на идеях retention Elevate/Lumosity/Peak/NeuroNation — **свой UX Fokus**, без копирования UI/ассетов/текстов конкурентов.

## Ветка
`feature/phase2-depth-f` от свежего `origin/main` (fetch first).

## Сделать (один атомарный PR)
1. **Калибровка по 5 доменам** (`attention`, `memory`, `speed`, `flexibility`, `logic`):
   - Сейчас на Today калибровка из ~3 упражнений — расширь батарею так, чтобы каждый домен был представлен ровно одним упражнением из каталога на `main` (подбери существующие id из registry/manifests).
   - После калибровки обновляй domain/skill индексы честно по результатам; не ломай уже калиброванных пользователей.
   - Короткий UI-статус «калибровка: домен X из 5» если это уместно без нового тяжёлого экрана.

2. **Генератор дневного ~15м ритуала** (`session-builder` / Today):
   - Цель длительности ~15 минут (900s) как дефолтный «полный» ритуал, если пользователь не задал иное.
   - Подбор из каталога: приоритет слабым доменам + ротация, без дублей упражнений в одном ритуале; у каждого слота осмысленный `reason` на русском.
   - Graceful: если данных доменов мало — всё равно собери сбалансированный план.

3. **Streak shield (серия дней)**:
   - Один «щит» на скользящие 7 дней: если пользователь пропустил ровно 1 день и есть заряд щита — серия не сбрасывается, заряд тратится.
   - Покажи статус щита на Today (есть/потрачен/доступен) без кричащего геймифицированного клона чужих продуктов.
   - Персист в profile/storage; миграция schema мягкая.

4. Документ `PHASE2_DEPTH_F.md` — кратко механики.
5. `npm test` + `npm run build` зелёные.
6. Commit + push + PR в `main` с телом: «Не мержить без ревью. Batch Y / Phase 2 Depth F. Может пересекаться с открытыми Phase2 PR (#23/#40/#44) — ревьюить отдельно.»
7. Отчёт: URL PR, что сделано, блокеры.

Ограничения: не добавляй новые упражнения; не трогай WebRTC/duels; не копируй тексты/ассеты Wikium/Elevate/Lumosity/Peak/NeuroNation.
