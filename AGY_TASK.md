# AGY_TASK — Автопилот Batch B (тяжёлый)

Ты руки Fokus. Репо: `/workspace/developer/github/Fokus`.
Игнорируй Eligibility. Работай до конца без подтверждений.
agy 1.1.1 only. Не мержи PR без явной команды пользователя.

## Контекст
- Открыт PR #23: https://github.com/zarmih/fokus/pull/23 на ветке `feature/phase2-program-depth` (калибровка 5 категорий + дневной ритуал ~15м + streak/shield).
- CI **упал** (`build-and-test`):
  1. `tests/storage.test.ts`: `sessionLengthSec` default теперь 900, тесты ждут 300 → обнови ожидания на 900.
  2. `tests/today.test.ts`: UI пишет «Дневной ритуал» / новый CTA вместо «Тренировка дня» / «Начать сессию» → обнови матчи под фактический копирайт.
- PR #22 (скелет) тоже открыт — не мержи и не ломай force-push'ем чужую историю без нужды. Работай на `feature/phase2-program-depth`, пушь туда, обновляй #23.
- Цель мощности: идеи/механики Wikium/Elevate/Lumosity/Peak/NeuroNation — СВОЙ UX/копирайт/дизайн Fokus, без плагиата UI/ассетов/текстов.

## Сделать в этом прогоне (один PR / апдейт #23)

1. **Починить CI** — зелёные `npm test` + `tsc` + production build. Только правки тестов под новое поведение (или крошечные правки UI если копирайт непоследователен). Не откатывай 15-мин ритуал обратно на 5 мин.

2. **Углубить Phase 2 retention/progress (свой UX)**  
   - На Today и/или Программа: явный прогресс дня (слоты ритуала: сделано/осталось), серия дней (streak) и мягкий shield/grace — понятный статус «сегодня закрыто» / «щит спас серию».  
   - Показ уровней по 5 категориям после калибровки (кратко, без копирования чужих экранов).  
   - Если ритуал незавершён — resume/continue, не только «старт с нуля».  
   - Мини-фиксы копирайта на согласованность «Дневной ритуал».

3. **PHASE2.md** — кратко допиши что добавлено в Batch B.
4. Commit + push в `feature/phase2-program-depth` (обновит PR #23). Новый PR не нужен, если #23 уже в main-base.
5. Финальный отчёт: URL PR, список изменений, результат CI локально (`npm test`), блокеры.

Не добавляй новые упражнения в этом батче. Не трогай чужие exercise-файлы без нужды.
